using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InfiniteSpawnerComponent : SpawnerBehavior
{
    //public override void SpawnSplash()
    //{

    //    if (Setup.spawnSetup[0] != null)
    //    {
    //        int i = 0;
    //        Instantiate(Setup.spawnSetup[0].splashList[i].splashType, gameObject.transform.position, gameObject.transform.rotation);
    //        i++;
    //        if (i >= Setup.spawnSetup[0].splashList.Length - 1 || stopSpawning)
    //        {
    //            CancelInvoke("SpawnSplash");
    //        }
    //    }



    //}

    //public override void SpawnSplash()
    //{
    //    if (Setup.spawnSetup[0] != null)
    //    {
    //        Instantiate(splashPrefab, Setup.spawnSetup[0].target.transform.position, Setup.spawnSetup[0].target.transform.rotation, splashContainer.transform);

    //    }
    //}
}
